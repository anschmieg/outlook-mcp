{
  "name": "outlook-mcp",
  "version": "2.0.0",
  "description": "MCP server for Claude to access Outlook data via Microsoft Graph API (HTTP transport)",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev src/worker.js",
    "deploy": "wrangler deploy src/worker.js",
    "deploy-script": "./scripts/deploy.sh",
    "test": "jest"
  },
  "keywords": [
    "claude",
    "outlook",
    "mcp",
    "microsoft-graph",
    "email",
    "cloudflare-workers",
    "http"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.16.6",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "wrangler": "^4.34.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
